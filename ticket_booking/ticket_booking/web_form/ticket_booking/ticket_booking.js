frappe.ready(function() {
    // Find the submit button in the Web Form and change its label to "Save"
    // $('button.submit-button').text('Save');
	frappe.web_form.set_value('email', 'annnnnnn')


	
});
